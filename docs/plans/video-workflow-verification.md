# 视频画布验收记录

验收日期：2026-07-12

## 已通过

- 效果图：`video-canvas-main-v1.png` 为 1440×900 RGB PNG，五区布局经人工视觉确认。
- Go：`go test -count=1 ./...`、`go test -race -count=1 ./internal/videoworkflow ./internal/videogen ./cmd/server`、`go vet ./...` 全部通过，Linux 静态编译成功。
- Web：Vitest 11 个文件共 59 项通过；Mock 契约 5 项通过；生产构建转换 1935 个模块并成功产出。
- E2E 契约：5 项通过，覆盖登录、19 节点模板、素材上传/变换/签名、10 次幂等运行、取消、Range 播放与 H.264/AAC 媒体探测。
- 浏览器：1440×900 主工作区、S02 下游失效、图片旋转新版本、时间线排序恢复、55.8 秒总时长、自动保存、完整运行、预览及下载均通过。
- FFmpeg：9:16、16:9、1:1 的 720p/1080p 六组编码均通过；四片段裁剪成片为 55.8 秒，误差不超过 50ms。
- MySQL：MySQL 8.0.41 执行迁移 Up 后生成 10 张表，并验证 `(user_id, request_id)` 与工作流积分流水幂等唯一索引；Down 后 10 张表全部移除。
- 部署静态检查：quick sync、备份和 entrypoint 脚本通过 `bash -n`；安全变量齐全时 Compose 展开成功，空白 `.env.example` 展开失败；生产配置拒绝默认/占位密钥、示例域名、localhost 与私网地址。
- 备份校验：数据库＋素材卷快照通过外层/内层 SHA-256、gzip、tar 路径安全及 `storage_key/sha256/size` 逐项校验；最新远端快照为 `video-workflow-snapshot-20260712T004720Z.tar.gz`，包含本次 Wan 运行已落库素材。
- 独立复审：前后端无残余 P0/P1；历史视频任务恢复按任务快照渠道重新读取对应凭据，不持久化密钥。

## 远端发布与真实渠道

- 当前生产入口为 `https://ai.reeko.net.cn:8000`（解析至 `64.83.17.240`）；外部 `/healthz`、`/readyz` 与页面静态资源通过，MySQL、Redis、server 健康，nginx 正常运行。
- 生产容器以 UID 10001 运行，内置 FFmpeg 6.1.1；MySQL 需应用 goose `20260711000100`、`20260712000100`，共 10 张 `video_workflow%` 表。
- 生产视频渠道已切换为 `apiyi_wan27`，内置古风模板升级为 v3，前后端默认模型与节点标签统一为 `wan2.7-r2v`；显式非 Wan 模型仍保持原值，渠道不匹配会在请求上游前失败。
- API易 Wan2.7 独立真实烟测通过：H.264、1280×720、30fps、AAC 双声道、5.038005 秒；官方能力文档确认参考图最多 5 张、720P/1080P、9:16，以及无参考视频时 2–15 秒，生产模板固定传入 4 张有序参考图。
- 视频引用图按目标端口固定为背景、女主、男主、表小姐；输入哈希同步使用端口顺序，历史 provider-aware/no-provider 哈希可恢复但禁用缓存，非视频节点哈希保持兼容。
- 视频缓存键已纳入脱敏后的渠道快照，fast/standard 与不同渠道不会误用旧产物；旧在途任务继续按持久化快照恢复。
- 内置古风 v3 模板已固化二维国风虚构成年人提示词、9:16 单镜头非拼贴背景，以及第二段 1200–12000ms、总时长 55.8 秒。

## 当前外部阻塞

- Wan2.7 四幕运行 `vwr_b1582adb-6f37-4a84-b2a7-844cb376e5ef` 已完成 3 个角色、四幕脚本、4 张背景和前 3 段视频；三段视频均为 1080×1920、15.023 秒并已落库，第四段提交返回 HTTP 403 `insufficient quota`，因此时间线与最终合成尚未执行。
- 失败运行已按 16 个成功节点结算 5310000 积分，预留记录为 `settled` 且 `credit_frozen=0`；Wan 渠道补充额度后，新运行可复用已完成媒体缓存，仅需完成第四段、时间线和合成，再以 ffprobe 验收 H.264、1080×1920、30fps、AAC 48kHz 双声道、55.8±0.05 秒即可关闭 REQ-011。
